<Window x:Class="NetCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:NetCapture.Converters"
        Title="NetCapture — Network Traffic Analyzer" 
        Height="800" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        Foreground="#CDD6F4">

    <Window.Resources>
        <converters:HexDumpConverter x:Key="HexDumpConverter"/>
        <converters:ProtocolColorConverter x:Key="ProtocolColorConverter"/>

        <!-- Scrollbar styling -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#2A2A3C"/>
        </Style>

        <!-- TreeView Item Style -->
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, Courier New"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="2,1"/>
            <Setter Property="IsExpanded" Value="True"/>
        </Style>

        <!-- Button style -->
        <Style TargetType="Button" x:Key="ToolbarButton">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#45475A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#585B70"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, Courier New"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#BAC2DE"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45475A"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#313244"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══════════════ TOOLBAR ═══════════════ -->
        <Border Grid.Row="0" Background="#181825" Padding="10,8" BorderBrush="#313244" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Interface selector -->
                <TextBlock Grid.Column="0" Text="Interface:" Foreground="#A6ADC8" 
                           VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding NetworkInterfaces}" 
                          SelectedItem="{Binding SelectedInterface}"
                          IsEnabled="{Binding IsCapturing, Converter={StaticResource InvertBoolConverter}}"
                          FontSize="12" Padding="6,4"
                          Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A"/>

                <!-- Start/Stop/Clear -->
                <Button Grid.Column="2" Content="▶ Start" 
                        Command="{Binding StartCaptureCommand}"
                        IsEnabled="{Binding IsCapturing, Converter={StaticResource InvertBoolConverter}}"
                        Style="{StaticResource ToolbarButton}" Margin="8,0,4,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="3" Content="⏹ Stop" 
                        Command="{Binding StopCaptureCommand}"
                        IsEnabled="{Binding IsCapturing}"
                        Style="{StaticResource ToolbarButton}" Margin="4,0,4,0"/>

                <Button Grid.Column="4" Content="🗑 Clear" 
                        Command="{Binding ClearCaptureCommand}"
                        Style="{StaticResource ToolbarButton}" Margin="4,0,0,0"/>

                <!-- Filter -->
                <TextBlock Grid.Column="6" Text="🔍 Filter:" Foreground="#A6ADC8" 
                           VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                <TextBox Grid.Column="7" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="12" Padding="8,5"
                         Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A" 
                         CaretBrush="#CDD6F4"/>
            </Grid>
        </Border>

        <!-- ═══════════════ PACKET LIST ═══════════════ -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding FilteredPackets}" 
                  SelectedItem="{Binding SelectedPacket}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No." Binding="{Binding Number}" Width="70"/>
                <DataGridTextColumn Header="Time" Binding="{Binding TimestampFormatted}" Width="140"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="180"/>
                <DataGridTextColumn Header="Destination" Binding="{Binding Destination}" Width="180"/>
                <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#89B4FA"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Length" Binding="{Binding Length}" Width="70"/>
                <DataGridTextColumn Header="Info" Binding="{Binding Info}" Width="*"/>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" 
                            Value="{Binding Protocol, Converter={StaticResource ProtocolColorConverter}, ConverterParameter=Background}"/>
                    <Setter Property="Foreground" 
                            Value="{Binding Protocol, Converter={StaticResource ProtocolColorConverter}, ConverterParameter=Foreground}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#45475A"/>
                            <Setter Property="Foreground" Value="#CDD6F4"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Splitter 1 -->
        <GridSplitter Grid.Row="2" Height="4" HorizontalAlignment="Stretch" 
                      Background="#313244" ResizeDirection="Rows" Cursor="SizeNS"/>

        <!-- ═══════════════ PACKET DETAILS TREE ═══════════════ -->
        <Border Grid.Row="3" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#181825" Padding="10,5" BorderBrush="#313244" BorderThickness="0,0,0,1">
                    <TextBlock Text="Packet Details" Foreground="#A6ADC8" FontSize="12" FontWeight="SemiBold"/>
                </Border>

                <TreeView Grid.Row="1" 
                          ItemsSource="{Binding SelectedPacket.DetailNodes}"
                          Background="#1E1E2E" 
                          BorderThickness="0"
                          Padding="4">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Label}" Padding="2,1"/>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>

                <TextBlock Grid.Row="1" 
                           Text="Select a packet to view details" 
                           Foreground="#585B70" FontStyle="Italic" FontSize="13"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding SelectedPacket, Converter={StaticResource NullToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Splitter 2 -->
        <GridSplitter Grid.Row="4" Height="4" HorizontalAlignment="Stretch" 
                      Background="#313244" ResizeDirection="Rows" Cursor="SizeNS"/>

        <!-- ═══════════════ HEX DUMP ═══════════════ -->
        <Border Grid.Row="5" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#181825" Padding="10,5" BorderBrush="#313244" BorderThickness="0,0,0,1">
                    <TextBlock Text="Hex Dump" Foreground="#A6ADC8" FontSize="12" FontWeight="SemiBold"/>
                </Border>

                <ScrollViewer Grid.Row="1" 
                              HorizontalScrollBarVisibility="Auto" 
                              VerticalScrollBarVisibility="Auto"
                              Background="#11111B">
                    <TextBlock Text="{Binding SelectedPacket.RawData, Converter={StaticResource HexDumpConverter}}" 
                               FontFamily="Cascadia Mono, Consolas, Courier New" 
                               FontSize="12" 
                               Foreground="#A6E3A1"
                               Padding="10,6"
                               TextWrapping="NoWrap"/>
                </ScrollViewer>

                <TextBlock Grid.Row="1" 
                           Text="Select a packet to view hex dump" 
                           Foreground="#585B70" FontStyle="Italic" FontSize="13"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding SelectedPacket, Converter={StaticResource NullToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- ═══════════════ STATUS BAR ═══════════════ -->
        <Border Grid.Row="6" Background="#181825" Padding="10,5" BorderBrush="#313244" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Margin="0,0,6,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#585B70"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCapturing}" Value="True">
                                        <Setter Property="Fill" Value="#A6E3A1"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding StatusText}" Foreground="#A6ADC8" FontSize="12"/>
                </StackPanel>

                <TextBlock Grid.Column="2" Foreground="#A6ADC8" FontSize="12">
                    <Run Text="Packets: "/>
                    <Run Text="{Binding PacketCount, Mode=OneWay}" FontWeight="SemiBold" Foreground="#89B4FA"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
